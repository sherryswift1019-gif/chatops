# ============================================================
# ChatOps GitLab CI — 构建并推送 Docker 镜像到 Harbor
# ============================================================
# Runner: zta-vm (amd64, Docker executor, socket binding)
# Harbor: harbor.paraview.cn（自签证书，runner daemon.json 已配 insecure-registries）
# CI Variables: HARBOR_USERNAME, HARBOR_PASSWORD
# ============================================================

variables:
  HARBOR_REGISTRY: harbor.paraview.cn
  HARBOR_PROJECT_CHATOPS: chatops
  HARBOR_PROJECT_PUBLIC: para-pam
  BASE_IMAGE_NAME: chatops-base
  APP_IMAGE_NAME: chatops
  # 禁用 BuildKit：BuildKit 不走 daemon 的 insecure-registries，传统 builder 走
  DOCKER_BUILDKIT: "0"
  # Deploy
  DEPLOY_HOST: "10.10.1.166"
  DEPLOY_DIR: "/opt/chatops"
  BACKUP_RETAIN_COUNT: "10"

stages:
  - build-base
  - test
  - qi-eval
  - build-app
  - deploy

# ============================================================
# 公共模板：使用 runner 的 Docker daemon（socket binding）
# ============================================================
.docker-job:
  image: ${HARBOR_REGISTRY}/${HARBOR_PROJECT_PUBLIC}/docker:27
  before_script:
    - echo "${HARBOR_PASSWORD}" | docker login ${HARBOR_REGISTRY} -u "${HARBOR_USERNAME}" --password-stdin
  tags:
    - zta-vm

# ============================================================
# Stage 1: 构建 base 镜像（仅依赖变更时触发）
# ============================================================
build-base:
  extends: .docker-job
  stage: build-base
  script:
    - |
      LOCK_HASH=$(sha256sum pnpm-lock.yaml | head -c 8)
      DEPS_TAG="deps-${LOCK_HASH}"
      FULL_BASE_LATEST="${HARBOR_REGISTRY}/${HARBOR_PROJECT_CHATOPS}/${BASE_IMAGE_NAME}:latest"
      FULL_BASE_DEPS="${HARBOR_REGISTRY}/${HARBOR_PROJECT_CHATOPS}/${BASE_IMAGE_NAME}:${DEPS_TAG}"

      echo "==> Building base image"
      echo "    latest: ${FULL_BASE_LATEST}"
      echo "    deps:   ${FULL_BASE_DEPS}"

      T0=$(date +%s)
      docker build \
        -f Dockerfile.base \
        -t "${FULL_BASE_LATEST}" \
        -t "${FULL_BASE_DEPS}" \
        .
      echo "==> [$(( $(date +%s) - T0 ))s] docker build done"

      T1=$(date +%s)
      docker push "${FULL_BASE_LATEST}"
      docker push "${FULL_BASE_DEPS}"
      echo "==> [$(( $(date +%s) - T1 ))s] docker push done"

      echo "==> Cleaning up local images on runner"
      docker rmi "${FULL_BASE_LATEST}" "${FULL_BASE_DEPS}" 2>/dev/null || true
      docker image prune -f
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - pnpm-lock.yaml
        - package.json
        - Dockerfile.base
        - web/pnpm-lock.yaml
        - web/package.json
      when: always
    - when: never

# ============================================================
# Stage 2: QA 质量门（轻量级 gate B 档）
# - 只保证"测试都过"，不设 coverage 阈值
# - 必须通过才进入 build-app / deploy，阻止缺陷合入 main
# - 触发场景：main 分支推送 + MR（需要 GitLab 项目开启 merge request pipelines）
# - 镜像前置条件：Harbor para-pam 项目需有 node:22-alpine 和 postgres:16-alpine 镜像
#   （如不存在：docker pull node:22-alpine → docker tag → docker push 到 harbor）
# ============================================================
test:
  stage: test
  image: ${HARBOR_REGISTRY}/${HARBOR_PROJECT_PUBLIC}/node:22-alpine
  services:
    - name: ${HARBOR_REGISTRY}/${HARBOR_PROJECT_PUBLIC}/postgres:16-alpine
      alias: postgres
  variables:
    POSTGRES_DB: chatops
    POSTGRES_USER: chatops
    POSTGRES_PASSWORD: chatops
    # 测试代码通过 resetTestDb() 自管 schema，不需要预跑 migrate
    DATABASE_URL: "postgres://chatops:chatops@postgres:5432/chatops"
    # pnpm 使用 store 加速后续构建；CI 缓存见下面 cache 段
    PNPM_HOME: /root/.pnpm-store
    # 内网 npm 代理：影响 corepack 下载 pnpm 及后续 pnpm install
    NPM_CONFIG_REGISTRY: "http://proxy.paraview.cn/repository/npm-proxy/"
  before_script:
    # alpine node 镜像不自带 pnpm；按 packageManager 指定版本安装
    - corepack enable
    - corepack prepare pnpm@10.33.0 --activate
    - pnpm config set store-dir "$PNPM_HOME"
    - pnpm config set registry http://proxy.paraview.cn/repository/npm-proxy/
    - pnpm install --frozen-lockfile
    - pnpm --dir web install --frozen-lockfile
  script:
    - echo "==> Backend typecheck"
    - pnpm typecheck
    - echo "==> Frontend typecheck"
    - pnpm --dir web typecheck
    - echo "==> Unit & integration tests (vitest)"
    - pnpm test
    - echo "==> Frontend build（含二次 tsc 作回归 + vite bundle 验证）"
    - pnpm --dir web build
  cache:
    key:
      files:
        - pnpm-lock.yaml
        - web/pnpm-lock.yaml
    paths:
      - .pnpm-store
      - node_modules
      - web/node_modules
    policy: pull-push
  rules:
    - if: '$CI_MERGE_REQUEST_ID'
      when: always
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: always
    - if: '$CI_COMMIT_TAG'
      when: always
    - when: never
  tags:
    - zta-vm

# ============================================================
# Stage 2.5: Quick-Impl 评测（v2 路线）
# ============================================================
# 文档：docs/prds/quick-impl-roles-v2/05-evaluation.md §4
#
# 三层模式：
#   - per-PR: standards lint + schema validation（轻量，秒级，每次跑）
#   - nightly: LLM-as-judge（涉及 Claude 网关，仅 schedule 触发）
#   - weekly: 滚动 baseline 趋势报告（仅 schedule + 周一触发）
#
# 触发文件：.claude/skills/quick-impl-artifact-author/** + docs/standards/** + scripts/qi-eval*
# ============================================================

# per-PR：standards lint（CLAUDE.md vs docs/standards/ 漂移检查）
qi-standards-lint:
  stage: qi-eval
  image: ${HARBOR_REGISTRY}/${HARBOR_PROJECT_PUBLIC}/node:22-alpine
  variables:
    PNPM_HOME: /root/.pnpm-store
    NPM_CONFIG_REGISTRY: "http://proxy.paraview.cn/repository/npm-proxy/"
  before_script:
    - corepack enable
    - corepack prepare pnpm@10.33.0 --activate
    - pnpm config set store-dir "$PNPM_HOME"
    - pnpm config set registry http://proxy.paraview.cn/repository/npm-proxy/
    - pnpm install --frozen-lockfile
  script:
    - echo "==> CLAUDE.md vs docs/standards/ 漂移检查"
    - pnpm exec tsx scripts/qi-standards-lint.ts
  rules:
    # 仅相关文件改动时触发；其他 PR 不浪费 runner
    - changes:
        - CLAUDE.md
        - docs/standards/**/*
        - .claude/skills/quick-impl-artifact-author/**/*
        - scripts/qi-standards-lint.ts
  cache:
    key:
      files:
        - pnpm-lock.yaml
    paths:
      - .pnpm-store
      - node_modules
  tags:
    - zta-vm

# per-PR：spec-author v3 输出 lint（warn-only 灰度，W1 上线，W4 切硬阻断）
# 触发：在 worktree 内有 spec-author 输出 JSON 或 spec.md 改动时
# 当前 allow_failure: true（warn-only 模式跑 --report，exit 0 但报告问题）
qi-spec-lint:
  stage: qi-eval
  image: ${HARBOR_REGISTRY}/${HARBOR_PROJECT_PUBLIC}/node:22-alpine
  variables:
    PNPM_HOME: /root/.pnpm-store
    NPM_CONFIG_REGISTRY: "http://proxy.paraview.cn/repository/npm-proxy/"
  before_script:
    - corepack enable
    - corepack prepare pnpm@10.33.0 --activate
    - pnpm config set store-dir "$PNPM_HOME"
    - pnpm config set registry http://proxy.paraview.cn/repository/npm-proxy/
    - pnpm install --frozen-lockfile
  script:
    - echo "==> qi-spec-lint：扫 docs/qi-eval-*-spec-author-*.json + fixtures"
    # 跑所有评测产物（嵌套 extendedOutput）和 fixtures（顶层 spec 结构）
    - |
      for f in $(find docs -maxdepth 2 -name 'qi-eval-*-spec-author-*.json' 2>/dev/null); do
        echo "→ $f (extracting extendedOutput)"
        node -e "const d=require('./$f');console.log(JSON.stringify(d.extendedOutput||d))" > /tmp/spec-flat.json
        pnpm exec tsx scripts/qi-spec-lint.ts --spec /tmp/spec-flat.json --report --json || true
      done
    - |
      for f in $(find src/__tests__/fixtures/spec-author -name '*.json' 2>/dev/null); do
        echo "→ $f"
        pnpm exec tsx scripts/qi-spec-lint.ts --spec "$f" --report --json || true
      done
  rules:
    - changes:
        - scripts/qi-spec-lint.ts
        - src/quick-impl/role-output-schemas.ts
        - src/__tests__/fixtures/spec-author/**/*
        - docs/qi-eval-*-spec-author-*.json
        - .claude/skills/quick-impl-artifact-author/roles/spec-author.md
  allow_failure: true
  cache:
    key:
      files:
        - pnpm-lock.yaml
    paths:
      - .pnpm-store
      - node_modules
  tags:
    - zta-vm

# nightly：LLM-as-judge 跑 baseline case（仅 schedule 触发）
# 配置 GitLab 项目 → Schedules，cron: 0 2 * * *（每日 02:00）
qi-eval-nightly:
  stage: qi-eval
  image: ${HARBOR_REGISTRY}/${HARBOR_PROJECT_PUBLIC}/node:22-alpine
  services:
    - name: ${HARBOR_REGISTRY}/${HARBOR_PROJECT_PUBLIC}/postgres:16-alpine
      alias: postgres
  variables:
    POSTGRES_DB: chatops
    POSTGRES_USER: chatops
    POSTGRES_PASSWORD: chatops
    DATABASE_URL: "postgres://chatops:chatops@postgres:5432/chatops"
    PNPM_HOME: /root/.pnpm-store
    NPM_CONFIG_REGISTRY: "http://proxy.paraview.cn/repository/npm-proxy/"
    # CLAUDE_CODE_OAUTH_TOKEN / ANTHROPIC_BASE_URL 由 GitLab CI Variables 注入
  before_script:
    - corepack enable
    - corepack prepare pnpm@10.33.0 --activate
    - pnpm config set store-dir "$PNPM_HOME"
    - pnpm config set registry http://proxy.paraview.cn/repository/npm-proxy/
    - pnpm install --frozen-lockfile
  script:
    - echo "==> Phase 5 Sprint：当前为框架就绪状态"
    - echo "    Phase 5+ 完成 LLM-as-judge 校准后启用真实 nightly run"
    - echo "    详见 docs/prds/quick-impl-roles-v2/05-evaluation.md §4"
    - echo ""
    - echo "==> 当前可跑：spec-author baseline case 评测（生成 report）"
    - echo "    pnpm exec tsx scripts/qi-eval.ts --role spec-author --case login-remember-me --mode v2-compact --execute"
    - echo ""
    - echo "==> 然后跑 LLM-as-judge 打分："
    - echo "    pnpm exec tsx scripts/qi-eval-judge.ts --report docs/qi-eval-...json"
    - echo ""
    - echo "（暂不在 CI 实际执行：Phase 5 仅就绪框架，校准 prompt 后开启）"
  artifacts:
    when: always
    paths:
      - docs/qi-eval-*.json
      - docs/qi-judge-*.json
    expire_in: 30 days
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule" && $QI_EVAL_NIGHTLY == "true"
  tags:
    - zta-vm

# ============================================================
# Stage 3: 构建业务镜像（main 推送或 tag 推送时触发）
# ============================================================
build-app:
  extends: .docker-job
  stage: build-app
  script:
    - |
      BASE_REF="${HARBOR_REGISTRY}/${HARBOR_PROJECT_CHATOPS}/${BASE_IMAGE_NAME}:latest"

      if [ -n "${CI_COMMIT_TAG:-}" ]; then
        APP_TAG="${CI_COMMIT_TAG}"
      else
        SHORT_SHA=$(echo "${CI_COMMIT_SHA}" | head -c 8)
        APP_TAG="${CI_COMMIT_REF_SLUG}-${SHORT_SHA}"
      fi
      FULL_APP="${HARBOR_REGISTRY}/${HARBOR_PROJECT_CHATOPS}/${APP_IMAGE_NAME}:${APP_TAG}"
      FULL_APP_LATEST="${HARBOR_REGISTRY}/${HARBOR_PROJECT_CHATOPS}/${APP_IMAGE_NAME}:latest"

      echo "==> Building app image"
      echo "    tag:    ${FULL_APP}"
      echo "    latest: ${FULL_APP_LATEST}"
      echo "    base:   ${BASE_REF}"

      T0=$(date +%s)
      docker build \
        --build-arg BASE_IMAGE="${BASE_REF}" \
        -t "${FULL_APP}" \
        -t "${FULL_APP_LATEST}" \
        .
      echo "==> [$(( $(date +%s) - T0 ))s] docker build done"

      T1=$(date +%s)
      docker push "${FULL_APP}"
      docker push "${FULL_APP_LATEST}"
      echo "==> [$(( $(date +%s) - T1 ))s] docker push done"

      echo "==> Cleaning up local images on runner"
      docker rmi "${FULL_APP}" "${FULL_APP_LATEST}" 2>/dev/null || true
      docker image prune -f
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: always
    - if: '$CI_COMMIT_TAG'
      when: always
    - when: never

# ============================================================
# Stage 4: 部署到生产服务器（手动触发）
# ============================================================
# CI Variables 需额外配置: DEPLOY_PASSWORD (Masked)
# 目标服务器需先运行 scripts/setup-target.sh 初始化
# ============================================================
deploy-prod:
  image: ${HARBOR_REGISTRY}/${HARBOR_PROJECT_PUBLIC}/docker:27-deploy
  extends: .docker-job
  stage: deploy
  resource_group: production
  script:
    - |
      # ── 计算镜像 tag（与 build-app 一致）──
      if [ -n "${CI_COMMIT_TAG:-}" ]; then
        APP_TAG="${CI_COMMIT_TAG}"
      else
        SHORT_SHA=$(echo "${CI_COMMIT_SHA}" | head -c 8)
        APP_TAG="${CI_COMMIT_REF_SLUG}-${SHORT_SHA}"
      fi
      FULL_IMAGE="${HARBOR_REGISTRY}/${HARBOR_PROJECT_CHATOPS}/${APP_IMAGE_NAME}:${APP_TAG}"
      echo "==> Deploying ${FULL_IMAGE} to ${DEPLOY_HOST}"

      export SSHPASS="${DEPLOY_PASSWORD}"
      SSH="sshpass -e ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR"
      SCP="sshpass -e scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR"

      # ── 传输 compose 文件和回滚脚本 ──
      ${SCP} docker-compose.prod.yml root@${DEPLOY_HOST}:${DEPLOY_DIR}/docker-compose.prod.yml
      ${SCP} scripts/rollback.sh root@${DEPLOY_HOST}:${DEPLOY_DIR}/rollback.sh
      ${SSH} root@${DEPLOY_HOST} "chmod +x ${DEPLOY_DIR}/rollback.sh"

      # ── 远程执行部署（heredoc 清晰分离本地/远程变量）──
      ${SSH} root@${DEPLOY_HOST} "bash -s" <<DEPLOY_EOF
      set -euo pipefail

      cd ${DEPLOY_DIR}
      mkdir -p backups

      # ── 1. 数据库备份 ──
      CURRENT_TAG=\$(grep '^CHATOPS_IMAGE=' .env 2>/dev/null | sed 's/.*://' || echo "none")
      if docker compose -f docker-compose.prod.yml ps postgres --status running 2>/dev/null | grep -q running; then
        BACKUP_FILE="backups/chatops_\$(date +%Y%m%d_%H%M%S)_\${CURRENT_TAG}.sql.gz"
        echo "==> Backing up database to \${BACKUP_FILE}"
        docker compose -f docker-compose.prod.yml exec -T postgres pg_dump -U chatops chatops | gzip > "\${BACKUP_FILE}"
        if [ ! -s "\${BACKUP_FILE}" ]; then
          echo "ERROR: Backup file is empty, aborting deploy"
          exit 1
        fi
        gzip -t "\${BACKUP_FILE}"
        echo "==> Backup OK: \$(du -h \${BACKUP_FILE} | cut -f1)"
      else
        echo "==> Postgres not running, skipping backup (first deploy?)"
      fi

      # ── 2. Harbor 登录 + 拉取镜像 ──
      echo "${HARBOR_PASSWORD}" | docker login ${HARBOR_REGISTRY} -u "${HARBOR_USERNAME}" --password-stdin
      docker pull ${FULL_IMAGE}

      # ── 3. 持久化镜像变量到 .env ──
      if grep -q '^CHATOPS_IMAGE=' .env 2>/dev/null; then
        sed -i "s|^CHATOPS_IMAGE=.*|CHATOPS_IMAGE=${FULL_IMAGE}|" .env
      else
        echo "CHATOPS_IMAGE=${FULL_IMAGE}" >> .env
      fi

      # ── 4. 记录部署历史 ──
      echo "\$(date +%Y%m%d_%H%M%S) ${APP_TAG} ${FULL_IMAGE}" >> .deploy-history

      # ── 5. 清理旧 migrate 容器并部署 ──
      docker compose -f docker-compose.prod.yml rm -f migrate 2>/dev/null || true
      docker compose -f docker-compose.prod.yml up -d

      # ── 6. 清理旧镜像 ──
      docker image prune -f 2>/dev/null || true

      # ── 7. 清理多余备份 ──
      cd backups
      ls -t chatops_*.sql.gz 2>/dev/null | tail -n +\$(( ${BACKUP_RETAIN_COUNT} + 1 )) | xargs -r rm -f || true
      DEPLOY_EOF

      # ── 健康检查（runner 端执行，max 120s）──
      echo "==> Waiting for health check (max 120s)..."
      ATTEMPT=0
      MAX_ATTEMPTS=24
      while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
        if ${SSH} root@${DEPLOY_HOST} "curl -sf http://localhost:3000/health" 2>/dev/null | grep -q '"ok"'; then
          echo "==> Health check passed after $(( ATTEMPT * 5 ))s"
          break
        fi
        ATTEMPT=$(( ATTEMPT + 1 ))
        if [ "$ATTEMPT" = "$MAX_ATTEMPTS" ]; then
          echo "==> Health check FAILED after 120s. Fetching logs..."
          ${SSH} root@${DEPLOY_HOST} "docker compose -f ${DEPLOY_DIR}/docker-compose.prod.yml logs --tail 30 chatops migrate"
          exit 1
        fi
        sleep 5
      done

      echo "==> Deploy complete: ${FULL_IMAGE}"
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: manual
      allow_failure: false
    - if: '$CI_COMMIT_TAG'
      when: manual
      allow_failure: false
    - when: never
  environment:
    name: production
    url: http://10.10.1.166:3000
  needs:
    - job: build-app
